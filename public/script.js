const mainHeading = document.getElementById('main-heading');
const button = document.getElementById('btn')

button.addEventListener('click', () => {
    mainHeading.textContent = "Magic has just started to happen!!!";
});